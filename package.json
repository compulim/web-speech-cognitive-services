{
  "name": "web-speech-cognitive-services",
  "version": "3.0.2-0",
  "description": "",
  "private": true,
  "main": "packages/component/lib/index.js",
  "types": "packages/component/lib/index.d.ts",
  "files": [
    "packages/component/lib/**/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run --stream build",
    "test": "lerna run --stream test",
    "watch": "lerna run --stream watch"
  },
  "dependencies": {
    "@babel/runtime": "^7.1.5",
    "event-as-promise": "^1.0.5",
    "events": "^3.0.0",
    "memoize-one": "^4.0.3",
    "microsoft-speech-browser-sdk": "^0.0.12",
    "simple-update-in": "^1.4.0"
  },
  "devDependencies": {
    "lerna": "^3.5.0"
  }
}
